import { UniquePlacesPipe } from './unique-places.pipe';

describe('UniquePlacesPipe', () => {
  it('create an instance', () => {
    const pipe = new UniquePlacesPipe();
    expect(pipe).toBeTruthy();
  });
});
